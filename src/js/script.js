document.addEventListener('DOMContentLoaded', function() {
    'use strict';

    // const portfolioItem = document.querySelectorAll('.portfolio__item'),
    //       portfilioBtn = document.querySelector('.portfolio__button');

    // openPortfolioItem();
    // portfilioBtn.addEventListener('click', openPortfolioItem);
    
    
    // for (let i = 0; i < portfolioItem.length; i++) {
    //     if (i < 6) {
    //         portfolioItem[i].classList.add('active');
    //     }
    // }
    function openPortfolioItem() {
        
        // const portfolioItemActive = document.querySelectorAll('.portfolio__item .active');
        // for (let i = 0; i < portfolioItem.length; i++) {
        //     if (portfolioItem[i].classList.contain('active')) {
        //         portfolioItem[i].classList.add('active');
        //     }
        // }
    }
});